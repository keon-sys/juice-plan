# 저장소의 텍스트 파일은 항상 LF로 저장한다.
# Windows(IntelliJ)와 WSL을 오가며 편집해도 줄바꿈만 바뀐 diff가 생기지 않게 한다.
* text=auto eol=lf

# 바이너리는 손대지 않는다.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.jar binary

# Windows 전용 스크립트는 CRLF를 유지해야 동작한다.
*.bat text eol=crlf
*.cmd text eol=crlf
